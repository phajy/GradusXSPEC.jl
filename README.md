# GradusXSPEC.jl

Use Gradus to evaluate models from XSPEC

## How to build the library

Typing `julia --project=. build_lib.jl` in the terminal will build the shared library and create the `model.dat` file.
